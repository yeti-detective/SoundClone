import React from 'react';
import UserHeaderBar from '../containers/user_header_bar_container';

const Stream = () => (
  <main>
    <UserHeaderBar />
    You can log out by clicking the three dots button on the far left of the header.
  </main>
);

export default Stream;
