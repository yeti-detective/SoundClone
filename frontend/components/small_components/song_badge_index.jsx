import React, { Component } from 'react';

export default class SongBadgeIndex extends Component {
  componentDidMount() {
    // get songs into state
  }

  render () {
    return (
      <section className="song-badge-index">
      </section>
    );
  }
}
